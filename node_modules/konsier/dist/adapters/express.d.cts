import { K as Konsier, N as NextFunction } from '../client-DE3LZ-YS.cjs';

interface ExpressLikeApp {
    post: (...args: any[]) => unknown;
}
declare function serveKonsier(app: ExpressLikeApp, konsier: Konsier): void;
declare function verifyKonsierPage(konsier: Konsier): (req: any, res: any, next?: NextFunction) => void;

export { type ExpressLikeApp, serveKonsier, verifyKonsierPage };
